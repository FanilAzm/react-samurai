let initialState = {
	friends: [
		{id: 1, name: 'Sasha'},
		{id: 2, name: 'Pasha'},
		{id: 3, name: 'Masha'}
	]
}

const sidebarReducer = (state = initialState, action) => {

	return state;
}

export default sidebarReducer;